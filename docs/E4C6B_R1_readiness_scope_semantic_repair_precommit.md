# E4C6B R1 — readiness-scope semantic repair

R0 established two structural classification errors in the frozen E4C6B attempt.

First, the current H operating object is explicitly a numerical **subcoordinate**. The same authority keeps the full H state incomplete, the full H architecture unselected, and the H_SERVICE/H_ACCESS automatic scalar disabled. R1 therefore preserves H representation/subcoordinate readiness but does not call it a full H scalar coordinate and does not mark H ready for a common scalar-component registry.

Second, E4C4 freezes exactly two PRIMARY I subcoordinates and two SENSITIVITY rows. R1 counts these roles separately. The two sensitivity rows are not promoted into the primary coordinate count.

C remains unresolved/evidence-only. K and D readiness are reused unchanged.

No economic result row, estimator, transform, inference value, component definition, or source artifact is changed. E4C6B and R0 remain preserved as historical attempts.
