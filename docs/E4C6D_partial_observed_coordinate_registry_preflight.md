# E4C6D — partial observed-coordinate registry preflight

This phase freezes the exact value lineage and extraction schema for the currently available five numerical coordinates before opening any source result row values.

Frozen coordinate set:

1. H — `H_ACCESS_SPACE_ROOMS_PER_PERSON`, current numerical H subcoordinate only.
2. K — `K_FIN_MEAN_TRANSFORMED`, full K scalar coordinate.
3. D — `D_PIRTOTAL_MEAN_STATE_TRANSFORMED`, full D scalar coordinate.
4. I — `I_FYFT_SHARE`, primary I subcoordinate.
5. I — `I_SEARCH_SECURITY`, primary I subcoordinate derived only through the already frozen state-sign orientation of `I_SEARCH_BURDEN_SHARE`.

All five are dimensionless and higher-is-better under previously frozen semantics. This does not imply five conceptual components: C remains unresolved and I contributes two coordinates.

The execution target is exactly 40 cell rows: five coordinates × four age bands × two tenures.

This preflight does not open economic result rows. It reads and validates only source headers and structural authorities. It freezes source paths, hashes, selectors, point and standard-error fields, state orientation, output schema, and exact expected shape.

No covariance, independence assumption, metric scaling, geometry, dimensionality test, real-inflation estimate, or final scalar is authorized.
