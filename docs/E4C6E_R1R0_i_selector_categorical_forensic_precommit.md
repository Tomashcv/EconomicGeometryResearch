# E4C6E R1R0 — I selector categorical forensic

E4C6E R1 successfully applied only the precommitted H age-token representation repair, then failed because both precommitted I primary-coordinate selectors selected zero rows.

This forensic does not repair I. It classifies the failure using only frozen categorical fields from the E4A2D I inference table:

- year
- age_band
- tenure
- estimand
- role
- state_sign

It does not interpret or emit `unweighted_n`, `point_estimate`, `replicate_variance`, `replicate_se`, or any economic result magnitude.

The forensic compares exact source tokens with the already frozen common-registry semantics. It separately diagnoses:

- PRIMARY role;
- each frozen primary estimand;
- exact versus semantically mappable age tokens;
- OWNER/RENTER tenure tokens;
- frozen state-sign tokens;
- cumulative selector counts.

No repair is authorized by this milestone. Any R2 mapping or selector repair must be justified from this frozen categorical evidence and precommitted before re-execution.
