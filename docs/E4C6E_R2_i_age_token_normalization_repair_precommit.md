# E4C6E R2 — I age-token normalization repair

The original failed E4C6E attempt, the H-only R1 attempt, and both categorical forensics remain immutable.

E4C6E R1R0 established without interpreting or emitting economic numeric values that both I primary selectors fail only at the common age-label representation boundary:

- the I source uses `AGE25_34`, `AGE35_44`, `AGE45_54`, `AGE55_64`;
- the common registry uses `25-34`, `35-44`, `45-54`, `55-64`;
- PRIMARY role matches;
- both expected source estimands match;
- OWNER/RENTER tenure labels match;
- +1 and -1 state-sign semantics match;
- after semantic age mapping, each I primary coordinate has exactly eight structurally eligible rows.

R2 therefore preserves the H age normalization from R1 and adds only the equivalent I source age-token bridge:

- `AGE25_34` -> `25-34`
- `AGE35_44` -> `35-44`
- `AGE45_54` -> `45-54`
- `AGE55_64` -> `55-64`

No estimator, state transform, state sign, component definition, source value, standard error, K/D selector, H selector semantics, I role/estimand/tenure selector, registry schema, or expected shape changes.

The frozen categorical signatures precommit exact I source-row identities:
- I_FYFT_SHARE: 2, 6, 10, 14, 18, 22, 26, 30
- I_SEARCH_SECURITY: 3, 7, 11, 15, 19, 23, 27, 31

After this precommit is committed and pushed, the patched executor may re-open the frozen sources and attempt the original 40-row registry. No outcome can affect pass/fail except the already frozen definitional finite-decimal and nonnegative-SE checks.
