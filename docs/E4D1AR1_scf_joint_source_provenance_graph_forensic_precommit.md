# E4D1AR1 — SCF joint-source provenance-graph forensic

E4D1AR proved that both `scfp2022s.zip` and `scf2022s.zip` are directly read by the frozen K/D executor, so an exactly-one-source assumption is not justified.

E4D1AR1 freezes three admissible effective source sets before deeper static inspection: summary-only, full-only, or joint summary+full. A joint set requires provenance, not mere dual file access. The AST/dataflow must show both candidate dependencies entering a downstream binding used for FIN/PIRTOTAL, or distinct mandatory target/structural roles entering the same combined estimator lineage.

A single-source set requires target-variable lineage for that source and no material target/structural role for the other source.

No raw SCF archive content or archive member listing may be opened. No numeric result row, 2019 data file, or economic value may be opened. If the static graph still cannot prove one unique source set, E4D1AR2 is the only next route and may be separately precommitted as a header-only frozen-2022 schema forensic.
