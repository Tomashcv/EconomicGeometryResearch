# E4D1AR2 R0 — Python environment repair

The preserved E4D1AR2 attempt failed during `pandas` import under system `python3`, before any ZIP member listing, Stata schema metadata, observation row, 2019 download, or economic value was opened.

R0 changes only the execution interpreter. The original E4D1AR2 contract, rules, archive lineage, and executor remain byte-for-byte frozen.

The only permitted candidate is the repository-local `.venv/bin/python`. No package install, pip invocation, system mutation, fallback interpreter, or executor edit is authorized.

After this precommit, the virtual environment may be probed for Python version, pandas version/import, StataReader import, and module/interpreter file hashes only. The resulting environment fingerprint must be committed and pushed before any archive member listing or Stata schema metadata is opened.

If the probe fails, preserve that failure and stop. If it passes, execute the original frozen E4D1AR2 executor under the frozen repository virtual environment.
