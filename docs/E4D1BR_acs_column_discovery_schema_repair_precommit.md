# E4D1BR — ACS frozen-column discovery repair and schema audit completion

E4D1B successfully downloaded and hash-pinned exactly six official 2019 artifacts, then failed before any observation/data-row parse because its ACS required-column discovery looked only for literal strings located directly inside AST subscript slices. That method returned an empty set.

E4D1BR repairs only the static source-lineage recovery method. It does not change the H estimand, source selection, URLs, downloaded bytes, SCF rules, CPS rules, or the no-value boundary.

The repaired ACS hierarchy is frozen before opening any new schema content:

1. Recover string-valued bindings from the frozen 2022 H executor by fixed-point AST propagation through assignments, lists, tuples, sets, dictionaries, names, subscripts, and call arguments.
2. Identify exact 2022 housing-header tokens used by DataFrame-style subscript indices and column-oriented helper calls.
3. If that effective set is empty, inspect the already-frozen H execution contract for exact tokens present in the 2022 housing header.
4. Add the pre-existing frozen ACS point/replicate weight architecture WGTP + WGTP1..WGTP80.
5. Require at least four non-weight effective ACS fields before accepting the recovery. This prevents a weight-only false pass.
6. Require every recovered field in every 2019 housing CSV header.

If this recovery is not unique/sufficient, E4D1BR remains blocked. No heuristic choice based on 2019 values is permitted.

After the ACS recovery, E4D1BR completes the same precommitted SCF metadata-only and CPS ZIP-member-only schema gates using the already-downloaded bytes. It never reads an ACS CSV data row, Stata observation row, or CPS ASCII line.

A full pass authorizes E4D1C. No 2019 coordinate value is opened in E4D1BR.
