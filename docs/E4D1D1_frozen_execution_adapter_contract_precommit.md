# E4D1D1 — frozen execution-adapter contract and static compatibility surface

E4D1D0 proved that all three frozen 2022 executors are top-level unsafe, with no CLI and no main guard. D1 therefore does not import or execute them. It freezes the only adaptation surface that D2 may use.

Scientific logic is immutable. Only enumerated source/container/path/year/output/hash/member bindings and schema adapters already frozen before 2019 outcomes may differ.

ACS: static ingestion compatibility layer using the validated HHLDRAGEP reconstruction from the exactly-one RELSHIPP=20 person's AGEP by SERIALNO, restricted to TYPE=1 AND NP>0. Person weights are prohibited.

SCF: static source/member compatibility layer using the frozen 2019 joint Summary+Full Y1 bridge plus replicate source. FIN/PIRTOTAL transforms, MI, and replicate inference remain frozen.

CPS: static fixed-width layout compatibility layer using only frozen 2019 public/replicate layout authorities. FYFT/SEARCH semantics and replicate logic remain frozen.

D1 inventories every top-level statement and top-level assignment, hashes all 17 frozen functions, and inspects only container/header metadata. It creates no executable 2019 adapter and opens zero 2019 coordinate values.
