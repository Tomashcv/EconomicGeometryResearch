# E4D1D2A0R — CPS multi-locus member-literal repair

A0 froze a safe unresolved result because its synthesis validator required each frozen 2022 CPS member literal to appear exactly once. Both member literals actually occur twice.

A0R freezes, before contextual inspection, the narrower structural hypothesis that each token has exactly two already-frozen D2 executable loci; the source line set must match those loci exactly; and both occurrences bind to the same already-selected 2019 member.

No new member may be selected. No scientific function, target, parser, transform, replicate rule, layout, or cohort definition may change. The only repair is multiplicity semantics: from one occurrence to all frozen loci of the same role.

The public and replicate tokens remain separate roles. Any extra occurrence, missing locus, line mismatch, cross-role mapping, or selected-member mismatch blocks A1.

A0R is source-only and opens zero 2019 semantic rows and zero coordinate values.
