# E4D1D2A0 — adapter source-synthesis blueprint freeze

D2/R1 resolved every container member mapping and every previously unidentified executable literal locus without opening 2019 values. The next risk is source assembly itself: the future adapter must preserve scientific logic while changing only the frozen compatibility surface.

A0 therefore freezes the exact top-level assembly blueprint before adapter source exists.

For each frozen 2022 method, every top-level AST statement is mapped to one of five policies:
- VERBATIM_IMPORT_OR_FUNCTION
- MUTABLE_ASSIGNMENT_BINDING
- MUTABLE_EXECUTABLE_LITERAL_STATEMENT
- IMMUTABLE_ASSIGNMENT
- IMMUTABLE_EXECUTABLE_STATEMENT

All 17 function bodies must match their D1 provenance hashes exactly.

Every mutable assignment comes from the D1 binding registry. Every mutable executable literal comes from the D2 locus registry. A0 records the exact parent statement source/hash for both.

For ACS, A0 additionally freezes the bridge insertion point immediately before the earliest top-level statement that consumes `HHLDRAGEP`. It does not create the bridge yet.

For SCF and CPS, A0 records exact old-member-literal to 2019-member rebinding pairs using the already frozen D2 member mappings.

No adapter is written or executed in A0, and no 2019 semantic row or coordinate value is opened.
