# E4D1D2A2 R1 — CPS static-layout authority target repair

The UTF-8 plumbing repair succeeded. A2 then constructed all three adapters, preserved all 17 frozen functions, and preserved the ACS accumulation suffix exactly, but routed unresolved because three CPS top-level bindings were not assigned 2019 targets.

Those bindings are not scientific variables and are not semantic data sources. `CPS_SAS`, `PERSON_LAYOUT`, and `HOUSE_LAYOUT` are static parsing/layout authorities. The generic D1 taxonomy classified the latter two as output bindings, while the A1 raw map intentionally covered only six semantic raw archives.

R1 freezes the repair before reading the E4D1C layout registry: resolve exactly three 2019 layout authority files from that already-frozen registry, replace exactly those three parent path literals, and replace exactly the three linked old-file SHA literals with the resolved target-file SHA values.

The parent CPS adapter remains immutable. R1 creates a new repaired CPS adapter. ACS and SCF remain byte-identical to A2.

No adapter is executed and no 2019 semantic row or coordinate value is opened.
